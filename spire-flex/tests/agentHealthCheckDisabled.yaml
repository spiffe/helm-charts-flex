suite: test .agent.healthCheck.enable false
set:
  agent: 
    healthChecks:
      enable: false
templates:
  - agent-configmap.yaml
tests:
  - it: should work
    set:
      agent.healthChecks.enable: false
    asserts:
      - containsDocument:
          apiVersion: "v1"
          kind: "ConfigMap"
          name: "RELEASE-NAME-agent-config"
          namespace: "NAMESPACE"
      - equal:
          path: data.agent_config
          value: |
            agent {
            }
            
            plugins {
            }
