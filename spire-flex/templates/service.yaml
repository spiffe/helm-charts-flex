apiVersion: v1
kind: Service
metadata:
  name: {{ include "spire-flex.fullname" . }}
  labels:
    {{- include "spire-flex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spire-flex.selectorLabels" . | nindent 4 }}
